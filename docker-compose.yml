# docker-compose.yml

services:
  xampp:
    image: tomsik68/xampp:latest
    container_name: uc-server
    privileged: true
    ports:
      - "8080:80"
    volumes:
      - /workspaces/UniConnect:/opt/lampp/htdocs/UniConnect:Z
    restart: unless-stopped
